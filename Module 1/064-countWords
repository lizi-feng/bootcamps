function countWords(str){
  var obj={};
  if(str!==''){
  var arr=str.split(' ');
  for(var i=0;i<arr.length;i++){
    if(typeof obj[arr[i]]!=='undefined'){
    obj[arr[i]]=obj[arr[i]]+1
    }else{
    obj[arr[i]]=1;
  }
  }
  }
  return obj;
}
